<%= simple_form_for post, html: { data: { action: 'ajax:success->posts#submitForm', target: 'posts.form' }}, remote: true do |f| %>
  <%= f.input :title %>
  <%= f.input :content %>
  <%= f.submit class:'btn btn-primary' %>
<% end %>
